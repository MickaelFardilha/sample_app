<!DOCTYPE html>
<meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
<html>
  <head>
    <title><%= titre %></title>
    <%= csrf_meta_tag %>
    <%= javascript_include_tag "application" %>
    <%= render 'layouts/stylesheets' %>
  </head>
    <%if @titre == "Liste des utilisateurs non sportifs" then %>
      <body onload="javascript:hideOrShowField('wds','none');hideOrShowField('wdsEntete','none')">
    <%else%>
      <body onload="javascript:hideOrShowField('wds','block');hideOrShowField('wdsEntete','block')">>
    <%end%>

    <div class="container">
      <%= render 'layouts/header' %>
      <section class="round">
        <%= yield %>
      </section>
      <%= render 'layouts/footer' %>
      <%= debug(params) if Rails.env.development? %>
    </div>
  </body>
</html>